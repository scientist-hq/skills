# Git Standards

## Repository Remote

The repository was renamed from `assaydepot/rx` to `scientist-hq/rx`. The git remote must point to `git@github.com:scientist-hq/rx.git` — never the old `assaydepot` remote.
