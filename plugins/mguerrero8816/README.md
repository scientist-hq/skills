# mguerrero8816 — Personal Claude Plugin

Personal Claude Code configuration for Mike Guerrero working on the RX Rails platform.

## Structure

```
mguerrero8816/
├── README.md               # This file — for human reviewers
├── SKILL.md                # Navigation index — Claude reads this to find skill files
├── CLAUDE.local.md         # Personal rules and preferences — Claude reads this at session start
├── rx-claude-local.md      # Reference copy of ~/rx/CLAUDE.local.md (see below)
├── sacred-rules/           # Always-on safety rules (SR-01 through SR-04)
└── skills/                 # On-demand skill files organized by category
    ├── pull-requests/
    ├── reviews/
    ├── tickets/
    ├── testing/
    ├── integrations/
    ├── debugging/
    ├── design/
    └── code-quality/
```

## How It Works

### Sacred Rules vs Skills

**Sacred rules** (`sacred-rules/`) are always active. They are injected into `~/rx/CLAUDE.local.md`
at every session start via a `SessionStart` hook in `~/rx/.claude/settings.local.json`:

```json
"hooks": {
  "SessionStart": [{
    "hooks": [{
      "type": "command",
      "command": "RULES=\"$HOME/skills/plugins/mguerrero8816/sacred-rules\" && CL=\"$HOME/rx/CLAUDE.local.md\" && { awk '/<!-- SACRED_RULES_INJECTION_POINT -->/{print;exit}{print}' \"$CL\" && printf \"\\n\" && cat \"$RULES/SR-01-no-remote-environments.md\" && printf \"\\n---\\n\\n\" && cat \"$RULES/SR-02-no-unauthorized-git-ops.md\" && printf \"\\n---\\n\\n\" && cat \"$RULES/SR-03-no-unauthorized-github-changes.md\" && printf \"\\n---\\n\\n\" && cat \"$RULES/SR-04-no-unsolicited-code-changes.md\" && printf \"\\n---\\n\\n\" && cat \"$RULES/SR-05-check-skill-routing.md\"; } > /tmp/claude_local_tmp.md && mv /tmp/claude_local_tmp.md \"$CL\" 2>/dev/null || true"
    }]
  }]
}
```

To update a sacred rule: edit the file in `sacred-rules/` — it takes effect at the next session start.

**Skills** (`skills/`) are on-demand. Claude reads a skill file only when the current task calls
for it, as directed by the routing tables in `SKILL.md`.

### How This Plugin Connects to the RX Project

`~/rx/CLAUDE.local.md` is a bootstrap loader that lives in the RX project directory. It is
gitignored (rx's own `.gitignore` handles it) and never checked into the repo.

Its only job is to tell Claude to read the four sacred rule files, `SKILL.md`, and
`CLAUDE.local.md` at the start of every session. It is not a rules file — all rules live here.

### ~/rx/CLAUDE.local.md — Exact Contents

This file is gitignored in the rx repo and never checked in. The sacred rules section
below the injection marker is auto-generated by the hook above — never edit it by hand.

```markdown
# Personal Claude Configuration

## ⛔ MANDATORY: Read This First
Before doing ANYTHING else — reading files, running commands, answering questions — read the following files in order:

1. `~/skills/plugins/mguerrero8816/sacred-rules/SR-01-no-remote-environments.md`
2. `~/skills/plugins/mguerrero8816/sacred-rules/SR-02-no-unauthorized-git-ops.md`
3. `~/skills/plugins/mguerrero8816/sacred-rules/SR-03-no-unauthorized-github-changes.md`
4. `~/skills/plugins/mguerrero8816/sacred-rules/SR-04-no-unsolicited-code-changes.md`
5. `~/skills/plugins/mguerrero8816/SKILL.md`
6. `~/skills/plugins/mguerrero8816/CLAUDE.local.md`

All six override any conflicting guidance in this project's CLAUDE.md. All personal skills
and references are managed in ~/skills/plugins/mguerrero8816/. Do not create or modify
Claude documentation anywhere else.

---

<!-- SACRED_RULES_INJECTION_POINT -->

[Sacred rules SR-01 through SR-04 are injected here automatically at session start]
```
